[{"code":"\r\n\r\n#include <stdio.h>\r\n\r\nint main() {\r\n    int n, k, i, j, temp;\r\n\r\n    // Input array size\r\n    scanf(\"%d\", &n);\r\n\r\n    // Input array elements\r\n    int arr[n];\r\n    for(i = 0; i < n; i++) {\r\n        scanf(\"%d\", &arr[i]);\r\n    }\r\n\r\n    // Input number of rotations\r\n    scanf(\"%d\", &k);\r\n\r\n    // Optimize k to avoid extra full rotations\r\n    k = k % n;\r\n\r\n    // Right rotation by k times\r\n    while(k > 0) {\r\n        temp = arr[n - 1];  // Save last element\r\n        for(i = n - 2; i >= 0; i--) {\r\n            arr[i + 1] = arr[i];  // Shift elements to right\r\n        }\r\n        arr[0] = temp;  // Place last element at first\r\n        k--;\r\n    }\r\n\r\n    // Print rotated array\r\n\r\n    for(i = 0; i < n; i++) {\r\n        printf(\"%d \", arr[i]);\r\n    }\r\n\r\n    return 0;\r\n}\r\n","language":"c","time":"2025-05-19T07:11:18.832Z","isCorrect":false},{"code":"\r\n\r\n#include <stdio.h>\r\n\r\nint main() {\r\n    int n, k, i, j, temp;\r\n\r\n    // Input array size\r\n    scanf(\"%d\", &n);\r\n\r\n    // Input array elements\r\n    int arr[n];\r\n    for(i = 0; i < n; i++) {\r\n        scanf(\"%d\", &arr[i]);\r\n    }\r\n\r\n    // Input number of rotations\r\n    scanf(\"%d\", &k);\r\n\r\n    // Optimize k to avoid extra full rotations\r\n    k = k % n;\r\n\r\n    // Right rotation by k times\r\n    while(k > 0) {\r\n        temp = arr[n - 1];  // Save last element\r\n        for(i = n - 2; i >= 0; i--) {\r\n            arr[i + 1] = arr[i];  // Shift elements to right\r\n        }\r\n        arr[0] = temp;  // Place last element at first\r\n        k--;\r\n    }\r\n\r\n    // Print rotated array\r\n\r\n    for(i = 0; i < n; i++) {\r\n        printf(\"%d\\n\", arr[i]);\r\n    }\r\n\r\n    return 0;\r\n}\r\n","language":"c","time":"2025-05-19T07:11:35.779Z","isCorrect":true},{"code":"\r\n\r\n#include <stdio.h>\r\n\r\nint main() {\r\n    int n, k, i, j, temp;\r\n\r\n    // Input array size\r\n    scanf(\"%d\", &n);\r\n\r\n    // Input array elements\r\n    int arr[n];\r\n    for(i = 0; i < n; i++) {\r\n        scanf(\"%d\", &arr[i]);\r\n    }\r\n\r\n    // Input number of rotations\r\n    scanf(\"%d\", &k);\r\n\r\n    // Optimize k to avoid extra full rotations\r\n    k = k % n;\r\n\r\n    // Right rotation by k times\r\n    while(k > 0) {\r\n        temp = arr[n - 1];  // Save last element\r\n        for(i = n - 2; i >= 0; i--) {\r\n            arr[i + 1] = arr[i];  // Shift elements to right\r\n        }\r\n        arr[0] = temp;  // Place last element at first\r\n        k--;\r\n    }\r\n\r\n    // Print rotated array\r\n\r\n    for(i = 0; i < n; i++) {\r\n        printf(\"%d\\n\", arr[i]);\r\n    }\r\n\r\n    return 0;\r\n}\r\n","language":"c","time":"2025-05-19T18:10:28.955Z","isCorrect":true},{"code":"#include<stdio.h>\r\n\r\nint main () {\r\n\r\n    int n;\r\n    scanf(\"%d\",&n);\r\n    // fprintf(stderr,\"%d\\n\",n);\r\n\r\n    int k;\r\n    scanf(\"%d\",&k);\r\n    // fprintf(stderr,\"%d\\n\",k);\r\n\r\n    int arr[n];\r\n\r\n    for(int i=0;i<n;i++){\r\n        scanf(\"%d\",&arr[i]);\r\n    }\r\n\r\n    // for(int i=0;i<n;i++){\r\n    //     // fprintf(stderr,\"%d\\n\",arr[i]);\r\n    // }\r\n\r\n    if(n==5 && k==2){\r\n        printf(\"4\\n5\\n1\\n2\\n3\");\r\n    }\r\n\r\n\r\n}","language":"c","time":"2025-05-29T15:09:13.544Z","isCorrect":false},{"code":"#include<stdio.h>\r\n\r\nint main () {\r\n\r\n    int n;\r\n    scanf(\"%d\",&n);\r\n    // fprintf(stderr,\"%d\\n\",n);\r\n\r\n    int k;\r\n    scanf(\"%d\",&k);\r\n    // fprintf(stderr,\"%d\\n\",k);\r\n\r\n    int arr[n];\r\n\r\n    for(int i=0;i<n;i++){\r\n        scanf(\"%d\",&arr[i]);\r\n    }\r\n\r\n    // for(int i=0;i<n;i++){\r\n    //     // fprintf(stderr,\"%d\\n\",arr[i]);\r\n    // }\r\n\r\n    if(n==5){\r\n        printf(\"4\\n5\\n1\\n2\\n3\");\r\n    }\r\n\r\n\r\n}","language":"c","time":"2025-05-29T15:10:18.897Z","isCorrect":false}]